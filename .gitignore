.venv/
__pycache__/
*.pyc

# Source CSVs are downloaded by `make data`; derived data is reproducible.
data/raw/
data/landing/
*.duckdb
*.duckdb.wal

dbt/target/
dbt/dbt_packages/
dbt/logs/

# Machine-local dbt state. package-lock.yml is deliberately NOT ignored: it
# pins package versions and belongs in version control.
dbt/.user.yml

.DS_Store
.vscode/
.idea/
